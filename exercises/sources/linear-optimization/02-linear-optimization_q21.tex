 Which of the following statements about a feasible basic solution is true for a minimization problem?

\begin{itemize}
\item[(a)] {If $\bar{c} < 0$, that is, all reduced costs are negative, the solution is optimal.}
\item[(b)] {If the values of the basic variables are negative, the solution is optimal.}
\item[(c)] {If $\bar{c}_N > 0$, that is, the reduced costs of the non basic variables are positive, the solution is optimal.}
\end{itemize}


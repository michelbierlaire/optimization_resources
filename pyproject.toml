[project]
name = "optimization-resources"
version = "0.1.0"
description = "Website for teaching optimization material"
authors = [
    { name = "Michel Bierlaire" }
]
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "mkdocs==1.6.1",
    "mkdocs-material==9.7.6",
    "pyyaml>=6.0"
]

[tool.uv]
package = false